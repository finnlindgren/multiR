# Add the contents of this file to your local .Rprofile
# if you need access to the INLA package

local({
  # Modify this as desired, for INLA and/or other extra repositories
  r <- c(INLA = "https://inla.r-inla-download.org/R/testing",
         CRAN = "https://cloud.r-project.org/")
    options(repos = r)
})
